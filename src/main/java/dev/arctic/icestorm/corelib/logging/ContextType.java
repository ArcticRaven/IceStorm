package dev.arctic.icestorm.corelib.logging;

/**
 * Determines display styling and severity grouping for console output.
 */
public enum ContextType {
    PLUGIN,
    INFO,
    DEBUG,
    WARNING,
    ERROR,
    CUSTOM
}
